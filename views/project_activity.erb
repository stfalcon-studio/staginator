<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <title>Staginator: <%= @current_project %></title>

    <!-- Bootstrap -->
    <link href="/vendor/bootstrap/css/bootstrap.min.css" rel="stylesheet">

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>
  <body>
    <%= ERB.new(File.read("#{settings.root}/views/navbar.erb"), 0, '-').result(binding) %>

    <div class="col-xs-12">
      <ul class="nav nav-tabs" role="tablist">
        <li role="presentation"><a href="/project/<%= @current_project %>">Stagings</a></li>
        <li role="presentation" class="active"><a href="/project/<%= @current_project %>/activity">Activity</a></li>
        <li role="presentation"><a href="/project/<%= @current_project %>/port_forwarding">Port forwarding</a></li>
        <li role="presentation"><a href="/project/<%= @current_project %>/info">Info</a></li>
      </ul>
      <br>
      <%= ERB.new(File.read("#{settings.root}/views/activity.erb"), 0, '-').result(binding) %>
    </div>
    <%= ERB.new(File.read("#{settings.root}/views/footer.erb"), 0, '-').result(binding) %>
    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.2/jquery.min.js"></script>
    <!-- Include all compiled plugins (below), or include individual files as needed -->
    <script src="/vendor/bootstrap/js/bootstrap.min.js"></script>
  </body>
</html>
