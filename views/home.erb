<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <title>Staginator: home</title>

    <!-- Bootstrap -->
    <link href="/vendor/bootstrap/css/bootstrap.min.css" rel="stylesheet">

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>
  <body>
  <%= ERB.new(File.read("#{settings.root}/views/navbar.erb"), 0, '-').result(binding) %>

      <% if flash[:project_remove] %>
        <div class="col-xs-12">
          <div class="alert alert-danger" role="alert" id="alert_success">
            <strong>In progress.</strong> Removing of project started.
            <div class="pull-right"><a href="#" id="close_alert_success"><span class="glyphicon glyphicon-remove" aria-hidden="true"></span></a></div>
          </div>
        </div>
      <% end %>

      <% if @not_found_404 %>
        <div class="col-xs-12">
          <div class="alert alert-warning" role="alert">
            <strong>This page doesn't exist.</strong> Try to select project above
          </div>
        </div>
      <% else %>
        <!--<div class="alert alert-success" role="alert">-->
          <!--<strong>Select the project above</strong>-->
        <!--</div>-->
        <%= ERB.new(File.read("#{settings.root}/views/activity.erb"), 0, '-').result(binding) %>
      <% end %>

    <%= ERB.new(File.read("#{settings.root}/views/footer.erb"), 0, '-').result(binding) %>
    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.2/jquery.min.js"></script>
    <!-- Include all compiled plugins (below), or include individual files as needed -->
    <script src="/vendor/bootstrap/js/bootstrap.min.js"></script>
    <script src="/js/close_alert.js"></script>
  </body>
</html>
